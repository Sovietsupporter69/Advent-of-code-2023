FileStream in1 = File.Open("Input_1.txt", FileMode.Open);
Console.WriteLine(in1.Length);
Console.WriteLine(in1.Length);
for (int i = 0;  i < in1.Length; i++) {
    string word = in1.ToString();
    for (int j = 0; j < in1.Length; j++) {

    }
}